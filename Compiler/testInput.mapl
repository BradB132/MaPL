
#import "./API.mapl"

#global bool isDebug;
#global float debugTimescale();
#type Hashable {
    int hash;
}
#type Object {
    string UUID;
    Object childForUUID(string);
}
#type Array: Object, Hashable {
    int count;
    Object [int];
}

for(int i = 0; i < 5; i++) {
    print("Hello World #" + i);
}
