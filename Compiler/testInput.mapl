
#import "./API.mapl"

#global bool isDebug;
#global float debugTimescale();
#type Hashable {
    int hash;
}
#type Object {
    string UUID;
    Object childForUUID(string);
}
#type Array<T>: Object, Hashable {
    int count;
    T [int];
}

for(int i = 0; i < 5; i++) {
    print("Hello World #" + i);
}

global.map["test"] = "ABC";
global.count++;
